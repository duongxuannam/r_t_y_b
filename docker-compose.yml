services:
  app:
    build: .
    environment:
      APP_ENV: production
      BIND_ADDR: 0.0.0.0:3000
      STATIC_DIR: /app/dist
    env_file:
      - back-end/.env.production
    ports:
      - "3000:3000"
