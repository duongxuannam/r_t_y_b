version: "3.9"

services:
  todo-api:
    image: ${IMAGE_REF}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      APP_ENV: production
      BIND_ADDR: 0.0.0.0:3000
      STATIC_DIR: /app/dist
    ports:
      - "3000:3000"
