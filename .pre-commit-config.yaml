repos:
  - repo: local
    hooks:
      - id: cargo-fmt
        name: cargo fmt
        entry: bash -lc 'cd back-end && cargo fmt --all'
        language: system
        types: [rust]
        pass_filenames: false
        stages: [pre-commit]
